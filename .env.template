# Apple App Store Connect API Credentials
# Required for Apple Analytics ETL Pipeline

# Apple Developer Team ID (find in App Store Connect -> Membership)
APPLE_TEAM_ID=YOUR_TEAM_ID_HERE

# Apple App Store Connect API Key ID (from API Keys section)
APPLE_KEY_ID=YOUR_KEY_ID_HERE

# Path to your AuthKey_XXXXXXXXXX.p8 file
APPLE_PRIVATE_KEY_PATH=/path/to/your/AuthKey_XXXXXXXXXX.p8

# Or alternatively, the private key content as string (base64 encoded)
# APPLE_PRIVATE_KEY_CONTENT=-----BEGIN PRIVATE KEY-----...

# Apple App Store Connect API Base URL (usually this)
APPLE_API_BASE=https://api.appstoreconnect.apple.com/v1

# App IDs to process (comma-separated, no spaces)
APP_IDS=1234567890,0987654321,1111111111

# AWS Configuration (if not using AWS CLI profiles)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_DEFAULT_REGION=us-east-1

# S3 Bucket for storing analytics data
S3_BUCKET=apple-analytics-pipeline

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Production settings
MAX_CONCURRENT_APPS=3
RETRY_ATTEMPTS=3
TIMEOUT_SECONDS=300

# Optional: Slack webhook for notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Optional: Email alerts
# ALERT_EMAIL=your-email@domain.com
